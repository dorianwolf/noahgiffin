<%= form_for([:admin, @live_client]) do |f| %>
  <% if @live_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@live_client.errors.count, "error") %> prohibited this live_client from being saved:</h2>

      <ul>
      <% @live_client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artist %><br>
    <%= f.text_field :artist %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :contact %><br>
    <%= f.text_field :contact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
