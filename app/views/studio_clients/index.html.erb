<p id="notice"><%= notice %></p>

<h1>Studio Clients</h1>

<% @studio_clients.each do |studio_client| %>
  <div class="studio-client">
    <div class="pic"><img src="<%= studio_client.image %>" alt="" /></div>
    <div class="artist"><%= studio_client.artist %></div>
    <div class="album"><%= studio_client.album %></div>
    <div class="role"><%= studio_client.role %></div>
    <% studio_client.tracks.each do |track| %>
      <div class="track"> <%= track.title %></div>
    <% end %>
    <!-- <p><%= link_to 'Show', studio_client %></p>
    <p><%= link_to 'Edit', edit_studio_client_path(studio_client) %></p>
    <p><%= link_to 'Destroy', studio_client, method: :delete, data: { confirm: 'Are you sure?' } %></p> -->
  </div>
<% end %>

<%= link_to 'New Studio client', new_studio_client_path %>
